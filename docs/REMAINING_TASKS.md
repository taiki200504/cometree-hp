# 未実装・未対応項目一覧

開発計画・本番チェックリスト・ポータル刷新メモを照合した、現時点で未実装の項目です。

---

## 1. 開発計画で未実装のもの

### コード・機能

| 項目 | 内容 | 優先度 |
|------|------|--------|
| **4.3 学生団体基礎知識ページ** | `/for-organizations/basics` などのページが未作成。コンテンツは将来追加可能なため、プレースホルダーページを作っておくか、文案が決まり次第で対応可。 | 低 |
| **5.3 CI と package.json の統一** | `package.json` の `engines`（例: node 22.x）と GitHub Actions の Node/pnpm バージョンを揃える。 | 推奨 |
| **5.4 join / community の実データ化** | ・`app/join/page.tsx` の「UNION連携コミュニティ」がモック（`partnerCommunities` 固定配列）のまま。API/DB から取得に変更する。・`app/community/page.tsx` の overview タブ内の団体一覧がモック（`organizations` 固定配列）のまま。`/api/organizations` 等から取得に変更する。 | 推奨 |
| **5.5 お問い合わせフォーム** | サイト内フォーム + API（バリデーション・Resend 等での送信）は未実装。現状は外部フォーム・mailto のみ。 | 要件次第 |
| **5.6 案件CRUD・学生向け一覧** | 企業が案件を登録し、学生が案件一覧を見る・応募する機能は未実装。テーブル設計・管理API・公開API・一覧ページの追加が必要。 | 要件次第 |
| **5.7 ユニメイトLPのHP内統合** | 現状は外部 `union-match-lp.vercel.app` へのリンクのみ。LPをこのリポジトリに統合する場合は `/lp/unimate` 等のルートを新設する。 | 方針次第 |
| **5.8 E2E テスト** | 管理画面・認証フローの E2E テストは未追加。 | オプション |
| **5.9 バックアップ・運用手順** | バックアップの取得タイミング・範囲・手順を `PRODUCTION_CHECKLIST.md` 等に明文化していない。必要ならスクリプトの自動化も検討。 | 推奨 |

### Discord まわり

| 項目 | 内容 | 優先度 |
|------|------|--------|
| **Discord ページ・ナビ** | 計画では「Slack と Discord の併記または移行」。現状はサービス・join/corporate の**文言**で Discord に言及しているが、**ナビには Slack のみ**（`/community/slack`）。`/community/discord` ページの作成や、ナビに「Discord」を追加・Slack と併記するかは未対応。 | 要件次第 |
| **Discord 招待リンク** | `NEXT_PUBLIC_DISCORD_INVITE_URL` を `env.example` に追加済み。本番で Discord を使う場合は環境変数を設定し、ボタン等でその URL を参照する必要あり（現状、該当リンクを張っているコンポーネントは要確認）。 | 本番で必要なら必須 |

---

## 2. 本番・運用で未対応のもの（PRODUCTION_CHECKLIST）

- [ ] **Vercel 設定** … プロジェクト作成・環境変数・ドメイン・SSL
- [ ] **監視・分析** … Google Analytics 4・Google Search Console・エラーアラート
- [ ] **デプロイ後確認** … トップ表示・管理画面・認証・画像アップロードの確認
- [ ] **運用監視** … エラーログ・パフォーマンス・アクセス解析・セキュリティ監視
- [ ] **定期メンテナンス** … 依存関係更新・セキュリティパッチ・DB最適化・バックアップ確認
- [ ] **緊急時対応** … 連絡先（開発・運用・緊急時）の記載

※ これらはコードではなく、デプロイ・運用の設定・手順です。

---

## 3. ポータル刷新メモで触れているが未実装のもの

- **拠点情報の詳細** … `/locations` は作成済み。BEGOPEN・クロスワン等の**具体的な拠点一覧・住所・マップ**は「順次お知らせ」レベルで、実データの追加・表示は未実装。
- **案件の実機能** … 「案件情報の登録」「案件を学生に流す」は、説明とお問い合わせ誘導まで。案件用テーブル・管理・公開一覧は未実装（上記 5.6 と同一）。

---

## 4. まとめ（実装してほしい場合の優先度イメージ）

1. **やったほうがよい（運用・一貫性）**  
   - 5.4 join / community の実データ化  
   - 5.9 バックアップ・運用手順の文書化  
   - 5.3 CI と package.json の統一  

2. **必要なら実装**  
   - 4.3 学生団体基礎知識ページ（URL だけ用意するか、コンテンツ付きで作るか）  
   - Discord 用ページ or ナビ追加・招待リンクの組み込み  
   - 5.5 お問い合わせフォーム（サイト内フォーム + API）  

3. **要件・方針が決まってから**  
   - 5.6 案件CRUD・学生向け一覧  
   - 5.7 ユニメイトLPのHP内統合  
   - 拠点の詳細データ・マップ  

4. **余裕があれば**  
   - 5.8 E2E テスト  

上記以外に「ここがまだ」という箇所があれば、その都度このメモに追記すると整理しやすいです。
